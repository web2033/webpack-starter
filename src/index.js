import './main.scss';

import trigger from '@web2033/trigger';
import anime from 'animejs';

/* Triggers to lazy-load images */

/* trigger({
  targets: 'img',
  margin: '20%',
  action(node) {
    node.src = node.dataset.src;
  }
}); */
